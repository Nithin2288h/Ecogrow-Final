"""Local shim package to allow deserialization of models that reference
the internal path `keras.src.models.functional`.

This package delegates to the installed `keras` package.
"""
# Intentionally empty shim package
